/** @type {import('next').NextConfig} */
const nextConfig = {
  // TODO: uncomment the below line if you want to build it with docker, since i use vercel for deployment, i didn't need to.
  // output: "standalone",
};

export default nextConfig;
